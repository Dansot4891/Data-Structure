/*#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

//프로젝트 - 속성 -일반 - sdl검사 아니오
//매니페스트검사- 입출력 - 매니페스트 포함 -아니오

void main()
{
	int a, b;
	int sum, dif, mul, div;
	int max, min;

	printf("숫자 입력 : ");
	scanf_s("%d", &a);

	printf("숫자 입력 : ");
	scanf_s("%d", &b);
	
	if (a > b) {

		dif = a - b;
		div = a / b;
	}
	else {

		dif = b - a;
		div = b / a;

	}
	sum = a + b;
	mul = a * b;

	printf("sum = %d\ndif = %d\nmul = %d\ndiv = %d\n", sum, dif, mul, div);
}*/
